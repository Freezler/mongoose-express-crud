<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>
    <%= product.name %>
  </title>
</head>

<body>
  <h1>
    <%= product.name %>
  </h1>
  <ul>
    <li>Price: €<%= product.price %>
    </li>
    <li>Category: <a href="/products?category=<%= product.category %>">
        <%= product.category %>
      </a>

    </li>
  </ul>

  <div class="display">
    <button type="button"><a href="/products/">All products</a></button>
    <button type="button"><a href="/products/<%= product._id %>/edit">Edit product</a></button>

    <form action="/products/<%= product._id %>?_method=DELETE" method="POST">
      <button type="submit">Delete product</button>
    </form>
  </div>
</body>

</html>

<style>
  .display {
    display: flex;
    justify-content: center;
    gap: 8px;
  }
</style>